#!/usr/bin/env python3
"""Annotate Stage 2 candidate-pool pairs with configured LLM relevance judges."""

import argparse
import json
import math
import os
import re
from pathlib import Path

from tqdm import tqdm

from pipeline_utils import (
    extract_query_number,
    list_query_json_files,
    load_benchmark_config,
    load_json_object,
    load_models_config,
    platform_from_model_entry,
    require_positive_int,
    write_json_atomic,
)

from queries_llm import build_evaluation_prompt

try:
    from dotenv import load_dotenv

    load_dotenv()
except ImportError:
    pass

_VERTEX_INITIALIZED = False
RUN_CONFIG_FILENAME = "annotation_run_config.json"

EVALUATION_REQUIRED_KEYS = {"main_resource", "scope", "supporting_field", "quote", "reason"}

EVALUATION_ALLOWED_KEYS = EVALUATION_REQUIRED_KEYS | {"relevance"}

STAGE2_HIT_FIELDS = (
    "id",
    "title",
    "description",
    "header",
    "content",
    "metadato_fileName",
    "resource_fileName",
    "retrieval",
)


def build_openai_evaluation_response_format(protocol):
    """Build the strict OpenAI JSON schema for one relevance evaluation."""
    return {
        "type": "json_schema",
        "json_schema": {
            "name": "dataset_relevance_evaluation",
            "strict": True,
            "schema": {
                "type": "object",
                "additionalProperties": False,
                "properties": {
                    "main_resource": {
                        "type": "string",
                    },
                    "scope": {
                        "type": ["string", "null"],
                    },
                    "supporting_field": {
                        "type": ["string", "null"],
                        "enum": protocol["positive_evidence_fields"] + [None],
                    },
                    "quote": {
                        "type": ["string", "null"],
                    },
                    # Binary relevance is not requested from the model because it is
                    # derived deterministically from the configured reason mapping.
                    "reason": {
                        "type": "string",
                        "enum": protocol["reason_labels"],
                    },
                },
                "required": [
                    "main_resource",
                    "scope",
                    "supporting_field",
                    "quote",
                    "reason",
                ],
            },
        },
    }


def validate_retrieval_provenance(retrieval, path, dataset_id):
    """Validate Stage 2 rank and score provenance for one pooled dataset."""
    if not isinstance(retrieval, dict) or not retrieval:
        raise ValueError(f"Invalid retrieval provenance for dataset {dataset_id!r} in {path}")

    for profile_name, profile_data in retrieval.items():
        if not isinstance(profile_name, str) or not profile_name.strip():
            raise ValueError(f"Invalid retrieval profile name for dataset {dataset_id!r} in {path}")

        if not isinstance(profile_data, dict):
            raise ValueError(
                f"Invalid retrieval data for profile {profile_name!r}, "
                f"dataset {dataset_id!r} in {path}"
            )

        rank = profile_data.get("rank")
        score = profile_data.get("score")

        if isinstance(rank, bool) or not isinstance(rank, int) or rank <= 0:
            raise ValueError(
                f"Invalid retrieval rank for profile {profile_name!r}, "
                f"dataset {dataset_id!r} in {path}: {rank!r}"
            )

        if (
            isinstance(score, bool)
            or not isinstance(score, (int, float))
            or not math.isfinite(float(score))
        ):
            raise ValueError(
                f"Invalid retrieval score for profile {profile_name!r}, "
                f"dataset {dataset_id!r} in {path}: {score!r}"
            )


def load_candidate_pool(path):
    """
    Load and validate one candidate-pool file generated by Stage 2.
    """
    data = load_json_object(path)

    filename_query_id = extract_query_number(Path(path).name)

    for required_key in ("id_query", "query", "hits"):
        if required_key not in data:
            raise ValueError(f"Candidate-pool file is missing {required_key!r}: {path}")

    query_id = require_positive_int(
        data["id_query"],
        f"{path}: id_query",
    )
    query = data["query"]
    hits = data["hits"]

    if query_id != filename_query_id:
        raise ValueError(
            f"Candidate-pool filename/query ID mismatch in {path}: "
            f"filename ID {filename_query_id} != id_query {query_id}"
        )

    if not isinstance(query, str) or not query.strip():
        raise ValueError(f"Invalid query text in {path}: {query!r}")

    query = query.strip()

    if not isinstance(hits, list):
        raise ValueError(f"'hits' must be a list in {path}")

    clean_hits = []
    seen_dataset_ids = set()

    for hit_index, hit in enumerate(hits, start=1):
        if not isinstance(hit, dict):
            raise ValueError(f"Hit #{hit_index} must be an object in {path}")

        missing_fields = [field for field in STAGE2_HIT_FIELDS if field not in hit]

        if missing_fields:
            raise ValueError(
                f"Hit #{hit_index} in {path} is missing required field(s): {missing_fields}"
            )

        dataset_id = hit["id"]

        if not isinstance(dataset_id, str) or not dataset_id.strip():
            raise ValueError(
                f"Hit #{hit_index} in {path} has invalid dataset id: {dataset_id!r}"
            )

        if dataset_id in seen_dataset_ids:
            raise ValueError(f"Duplicate dataset ID {dataset_id!r} in {path}")

        seen_dataset_ids.add(dataset_id)

        clean_hit = dict(hit)

        retrieval = clean_hit["retrieval"]

        validate_retrieval_provenance(retrieval, path, dataset_id)

        clean_hit["retrieval"] = retrieval
        clean_hits.append(clean_hit)

    return {"id_query": query_id, "query": query, "hits": clean_hits}


def clean_json_output(text):
    """
    Extract a JSON object from a model response while tolerating Markdown
    code fences and minor trailing-comma issues.
    """
    if not text:
        return ""

    text = text.strip()

    text = re.sub(r"^```(?:json)?\s*", "", text, flags=re.IGNORECASE)

    text = re.sub(r"\s*```$", "", text).strip()

    start = text.find("{")
    end = text.rfind("}")

    if start == -1 or end == -1:
        return text

    json_text = text[start : end + 1]

    json_text = json_text.replace("\n", " ").replace("\r", "")

    json_text = re.sub(r",\s*}", "}", json_text)

    json_text = re.sub(r",\s*\]", "]", json_text)

    return json_text.strip()


def generate_with_ollama(prompt, model_name, generation_config):
    """
    Generate one model response using Ollama.
    """
    import ollama

    kwargs = {
        "model": model_name,
        "prompt": prompt,
        "options": {
            "num_ctx": generation_config["num_ctx"],
            "temperature": generation_config["temperature"],
            "top_p": generation_config["top_p"],
            "repeat_penalty": generation_config["repeat_penalty"],
            "num_predict": generation_config["max_output_tokens"],
        },
    }

    if generation_config["structured_output"]:
        kwargs["format"] = "json"

    response = ollama.generate(**kwargs)

    return response.get("response", "").strip()


def generate_with_openai(messages, client, model_name, response_format):
    """
    Generate one structured evaluation using the OpenAI API.
    """
    response = client.chat.completions.create(
        model=model_name,
        messages=messages,
        response_format=response_format,
    )

    return response.choices[0].message.content


def initialize_vertex_ai():
    """
    Initialize Vertex AI once per process.
    """
    global _VERTEX_INITIALIZED

    if _VERTEX_INITIALIZED:
        return

    import vertexai

    project_id = os.getenv("VERTEX_PROJECT_ID") or os.getenv("GOOGLE_CLOUD_PROJECT")

    location = os.getenv("VERTEX_LOCATION", "us-central1")

    init_kwargs = {"location": location}

    if project_id:
        init_kwargs["project"] = project_id

    vertexai.init(**init_kwargs)

    _VERTEX_INITIALIZED = True


def generate_with_gemini(messages, model_name, generation_config):
    """
    Generate one structured evaluation using Gemini through Vertex AI.
    """
    from vertexai.generative_models import GenerationConfig, GenerativeModel

    prompt = messages[0]["content"]

    initialize_vertex_ai()

    model = GenerativeModel(model_name)

    response = model.generate_content(
        prompt,
        generation_config=GenerationConfig(
            temperature=generation_config["temperature"],
            top_p=generation_config["top_p"],
            max_output_tokens=generation_config["max_output_tokens"],
            response_mime_type=generation_config["response_mime_type"],
        ),
    )

    if hasattr(response, "text") and response.text:
        return response.text.strip()

    if hasattr(response, "candidates") and response.candidates:
        parts = response.candidates[0].content.parts

        if parts and hasattr(parts[0], "text"):
            return parts[0].text.strip()

    return ""


def generate_response(
    platform,
    prompt,
    model_name,
    generation_config,
    client=None,
    openai_response_format=None,
):
    """
    Generate a model response using the selected inference platform.
    """
    platform = platform.lower()

    if platform == "ollama":
        return generate_with_ollama(prompt, model_name, generation_config)

    if platform in ("openai", "gpt"):
        return generate_with_openai(
            [{"role": "user", "content": prompt}],
            client,
            model_name,
            openai_response_format,
        )

    if platform in ("gemini", "vertex"):
        return generate_with_gemini(
            [{"role": "user", "content": prompt}], model_name, generation_config
        )

    raise ValueError(f"Unknown platform: {platform}")


def build_run_provenance(
    model_name,
    platform,
    annotation_config,
    generation_config,
    protocol,
):
    """
    Build the reproducibility metadata stored for one annotation judge.

    The metadata captures the annotation protocol and backend generation
    settings required to verify that resumed runs are compatible.
    """
    return {
        "model": model_name,
        "platform": platform,
        "annotation": {
            "max_content": annotation_config["max_content"],
            "few_shot_examples": annotation_config["few_shot_examples"],
            "few_shots": annotation_config["few_shots"],
            "blueprint": annotation_config["blueprint"],
            "max_attempts": annotation_config["max_attempts"],
        },
        "protocol": {
            "input_components": list(protocol["input_components"]),
            "reason_labels": list(protocol["reason_labels"]),
            "relevance_mapping": dict(protocol["relevance_mapping"]),
            "positive_evidence_fields": list(
                protocol["positive_evidence_fields"]
            ),
            "negative_evidence_normalization": dict(
                protocol["negative_evidence_normalization"]
            ),
        },
        "generation": generation_config,
    }


def comparable_run_provenance(provenance):
    """
    Return the provenance fields that must remain identical across resumed runs.

    Maximum attempts is excluded because changing the retry budget does not
    change the protocol or generation settings of already valid annotations.
    """
    annotation = dict(provenance.get("annotation", {}))
    # Retry budget affects how long invalid outputs are retried, but not the
    # meaning of an already valid annotation.
    annotation.pop("max_attempts", None)

    return {
        "model": provenance.get("model"),
        "platform": provenance.get("platform"),
        "annotation": annotation,
        "protocol": provenance.get("protocol"),
        "generation": provenance.get("generation", {}),
    }


def load_run_config(output_dir):
    """Load stored annotation-run provenance, or initialize an empty registry."""
    path = Path(output_dir) / RUN_CONFIG_FILENAME

    if not path.exists():
        return {"models": {}}

    with path.open(encoding="utf-8") as file:
        payload = json.load(file)

    if not isinstance(payload, dict):
        raise ValueError(f"Annotation run metadata must be a JSON object: {path}")

    models = payload.get("models")

    if not isinstance(models, dict):
        raise ValueError(f"Annotation run metadata must contain a models object: {path}")

    return payload


def save_run_config(output_dir, run_config):
    """Persist annotation-run provenance atomically."""
    path = Path(output_dir) / RUN_CONFIG_FILENAME

    write_json_atomic(run_config, path)


def existing_valid_annotations_for_model(output_dir, model_name, protocol):
    """Return True when reusable valid annotations exist for the model."""
    output_dir = Path(output_dir)

    if not output_dir.exists():
        return False

    try:
        annotation_files = list_query_json_files(output_dir)
    except FileNotFoundError:
        return False

    for path in annotation_files:
        with path.open(encoding="utf-8") as file:
            payload = json.load(file)

        hits = payload.get("hits")

        if not isinstance(hits, list):
            raise ValueError(f"Existing annotation file has invalid hits list: {path}")

        for hit in hits:
            if not isinstance(hit, dict):
                raise ValueError(f"Existing annotation file has invalid hit object: {path}")

            annotations = hit.get("annotations", {})

            if not isinstance(annotations, dict):
                raise ValueError(f"Existing annotation file has invalid annotations object: {path}")

            evaluation = annotations.get(model_name)

            # Only one valid annotation is needed to require provenance compatibility:
            # otherwise a resumed run could mix old and new settings in the same model.
            if evaluation is not None and is_valid_evaluation(evaluation, protocol):
                return True

    return False


def ensure_run_provenance(output_dir, model_name, current_provenance, protocol):
    """Ensure existing annotations can be safely resumed with current settings."""
    output_path = Path(output_dir)

    output_path.mkdir(parents=True, exist_ok=True)

    has_valid_annotations = existing_valid_annotations_for_model(output_path, model_name, protocol)

    run_config = load_run_config(output_path)

    models = run_config.setdefault("models", {})

    stored_provenance = models.get(model_name)
    # Existing judgments without matching provenance cannot be safely reused,
    # because their prompt/protocol/generation settings cannot be verified.
    if has_valid_annotations and stored_provenance is None:
        raise ValueError(
            f"Existing valid annotations were found for {model_name!r}, "
            "but no annotation-run metadata is available. Safe resume cannot "
            "be verified; use a fresh output directory for a new run."
        )

    if stored_provenance is not None:
        # Prevent a single model's output files from mixing annotations produced
        # under different experimental settings.
        if comparable_run_provenance(stored_provenance) != comparable_run_provenance(
            current_provenance
        ):
            raise ValueError(
                "Existing annotations for model "
                f"{model_name!r} were generated with different settings. "
                "Stored provenance differs from current run provenance."
            )

        return

    models[model_name] = current_provenance

    save_run_config(output_path, run_config)


def dataset_input_fields(protocol):
    """Return the dataset fields exposed to the LLM judge."""
    return [
        component
        for component in protocol["input_components"]
        if component != "query"
    ]


def relevance_from_reason(evaluation, protocol):
    """Deterministically derive binary relevance from the reason label."""
    reason = evaluation.get("reason")

    if not isinstance(reason, str):
        return None

    return protocol["relevance_mapping"].get(reason.strip())


def normalize_evaluation(evaluation, protocol):
    """
    Normalize evaluation fields and derive binary relevance from the reason.

    Negative judgments also receive the configured null evidence values so
    equivalent model outputs share one canonical representation.
    """
    normalized = {}

    for key in ("main_resource", "scope", "supporting_field", "quote", "reason"):
        if key in evaluation:
            normalized[key] = evaluation[key]

    reason = evaluation.get("reason")

    if isinstance(reason, str):
        reason = reason.strip()
        normalized["reason"] = reason

        if protocol["relevance_mapping"].get(reason) is False:
            # Negative annotations do not require supporting evidence; normalize
            # these fields to the protocol-defined canonical values.
            for field, value in protocol["negative_evidence_normalization"].items():
                normalized[field] = value

    mapped_relevance = relevance_from_reason(evaluation, protocol)

    if mapped_relevance is not None:
        normalized["relevance"] = mapped_relevance

    return normalized


def validation_error(evaluation, protocol):
    """
    Return a validation error message, or None when the evaluation is valid.
    """
    if not isinstance(evaluation, dict):
        return "Evaluation is not a JSON object."

    if "error" in evaluation:
        return evaluation.get("error") or "Evaluation is marked as invalid."

    keys = set(evaluation.keys())

    missing_keys = EVALUATION_REQUIRED_KEYS - keys

    if missing_keys:
        return f"Missing required keys: {sorted(missing_keys)}"

    extra_keys = keys - EVALUATION_ALLOWED_KEYS

    if extra_keys:
        return f"Unexpected keys: {sorted(extra_keys)}"

    reason = evaluation.get("reason")

    if not isinstance(reason, str) or reason.strip() not in protocol["reason_labels"]:
        return f"Invalid reason: {reason!r}"

    reason = reason.strip()

    supporting_field = evaluation.get("supporting_field")

    quote = evaluation.get("quote")

    if protocol["relevance_mapping"][reason] is True:
        # Positive judgments must identify both an allowed supporting field and
        # a non-empty evidence quote from the candidate representation.
        if supporting_field not in protocol["positive_evidence_fields"]:
            return f"Invalid supporting_field for {reason!r}: {supporting_field!r}"

        if not isinstance(quote, str) or not quote.strip():
            return f"Missing or invalid quote for {reason!r}."

    return None


def is_valid_evaluation(evaluation, protocol):
    """
    Return True when an evaluation conforms to the annotation schema.
    """
    if not isinstance(evaluation, dict):
        return False

    if "error" in evaluation:
        return False

    normalized = normalize_evaluation(evaluation, protocol)

    return validation_error(normalized, protocol) is None


def parse_and_validate_evaluation(response_text, protocol):
    """
    Parse a raw model response and validate it against the annotation schema.
    """
    cleaned_response = clean_json_output(response_text)

    try:
        evaluation = json.loads(cleaned_response)

    except Exception:
        return (
            {
                "error": "invalid_json",
                "raw_output": cleaned_response,
            },
            "Invalid JSON.",
        )

    if not isinstance(evaluation, dict):
        error = "Evaluation is not a JSON object."
        return (
            {
                "error": "invalid_schema",
                "detail": error,
                "raw_output": cleaned_response,
            },
            error,
        )

    # Normalize before validation so all backends are judged against the same
    # canonical annotation representation.
    evaluation = normalize_evaluation(
        evaluation,
        protocol,
    )

    error = validation_error(evaluation, protocol)

    if error:
        return ({"error": "invalid_schema", "detail": error, "raw_output": cleaned_response}, error)

    return evaluation, None


def load_or_initialize_annotated_pool(input_pool, output_path, evidence_fields):
    """
    Load an existing annotated pool or initialize one from the Stage 2
    candidate pool.

    Existing annotation files must match the current candidate-pool dataset
    IDs exactly. This prevents annotations from being silently attached to
    a different retrieval result set.
    """
    if not output_path.exists():
        annotated_pool = {
            "id_query": input_pool["id_query"],
            "query": input_pool["query"],
            "hits": [],
        }

        for hit in input_pool["hits"]:
            annotated_hit = dict(hit)
            annotated_hit["annotations"] = {}

            annotated_pool["hits"].append(annotated_hit)

        return annotated_pool

    with open(output_path, "r", encoding="utf-8") as file:
        annotated_pool = json.load(file)

    if not isinstance(annotated_pool, dict):
        raise ValueError(f"Existing annotation file must contain a JSON object: {output_path}")

    for required_key in ("id_query", "query", "hits"):
        if required_key not in annotated_pool:
            raise ValueError(
                f"Existing annotation file is missing {required_key!r}: {output_path}"
            )

    if annotated_pool.get("id_query") != input_pool.get("id_query"):
        raise ValueError(f"Query ID mismatch in {output_path}")

    if annotated_pool.get("query") != input_pool.get("query"):
        raise ValueError(f"Query text mismatch in {output_path}")

    input_ids = [hit.get("id") for hit in input_pool["hits"]]

    annotated_hits = annotated_pool.get("hits")

    if not isinstance(annotated_hits, list):
        raise ValueError(f"Existing annotation hits must be a list: {output_path}")

    seen_output_ids = set()

    for hit_index, output_hit in enumerate(annotated_hits, start=1):
        if not isinstance(output_hit, dict):
            raise ValueError(
                f"Existing annotation hit #{hit_index} must be an object: {output_path}"
            )

        if "id" not in output_hit:
            raise ValueError(
                f"Existing annotation hit #{hit_index} is missing id: {output_path}"
            )

        dataset_id = output_hit["id"]

        if dataset_id in seen_output_ids:
            raise ValueError(
                f"Duplicate dataset ID {dataset_id!r} in existing annotation "
                f"file: {output_path}"
            )

        seen_output_ids.add(dataset_id)

        if "annotations" not in output_hit:
            raise ValueError(
                f"Existing annotation hit {dataset_id!r} is missing annotations: {output_path}"
            )

        if not isinstance(output_hit["annotations"], dict):
            raise ValueError(
                f"Existing annotation hit {dataset_id!r} has invalid annotations "
                f"object: {output_path}"
            )

    output_ids = [hit.get("id") for hit in annotated_hits]

    # Exact order is required because annotations are stored alongside the
    # Stage 2 hits rather than joined later by dataset ID.
    if input_ids != output_ids:
        raise ValueError(
            f"Candidate-pool dataset IDs differ from existing annotation file: {output_path}"
        )

    # Refresh Stage 2 provenance and representation fields while preserving
    # annotations only when the evidence shown to the judge is unchanged.
    for input_hit, output_hit in zip(input_pool["hits"], annotated_hits):
        annotations = output_hit["annotations"]

        changed_fields = [
            field
            for field in ("id", *evidence_fields)
            if input_hit.get(field) != output_hit.get(field)
        ]

        if changed_fields and annotations:
            raise ValueError(
                "Existing annotation would be reused with changed evidence: "
                f"query_id={input_pool.get('id_query')!r}, "
                f"dataset_id={input_hit.get('id')!r}, "
                f"changed_fields={changed_fields}, "
                f"output_file={output_path}"
            )

        output_hit.clear()
        output_hit.update(input_hit)

        output_hit["annotations"] = annotations

    return annotated_pool


def save_annotated_pool(annotated_pool, output_path):
    """
    Persist an annotated candidate pool.
    """
    write_json_atomic(annotated_pool, output_path)


def annotate_candidate_pool(
    platform,
    candidate_pool,
    annotated_pool,
    model_name,
    generation_config,
    client,
    protocol,
    openai_response_format,
    output_path,
    max_content_chars,
    few_shots,
    few_shot_examples,
    blueprint,
    max_attempts,
):
    """Annotate every pending query-dataset pair for one LLM judge."""
    fields = dataset_input_fields(protocol)

    pending_indices = []
    completed = 0

    for index, hit in enumerate(annotated_pool["hits"]):
        annotations = hit.setdefault("annotations", {})

        existing_evaluation = annotations.get(model_name)
        # Valid annotations are immutable on resume; only missing or invalid
        # judgments are submitted again.
        if existing_evaluation is not None and is_valid_evaluation(
            existing_evaluation,
            protocol,
        ):
            completed += 1
        else:
            pending_indices.append(index)

    print(
        f"{completed} result(s) already have a valid annotation "
        f"from {model_name}. "
        f"Processing {len(pending_indices)} pending result(s)."
    )

    for index in pending_indices:
        hit = annotated_pool["hits"][index]

        if few_shots:
            prompt = build_evaluation_prompt(
                candidate_pool["query"],
                hit,
                content_characters=max_content_chars,
                fields=fields,
                template=blueprint,
                fewshots=blueprint,
                few_shot_count=few_shot_examples,
            )
        else:
            prompt = build_evaluation_prompt(
                candidate_pool["query"],
                hit,
                content_characters=max_content_chars,
                fields=fields,
                template=blueprint,
            )

        evaluation = None
        last_validation_error = None
        # Retry malformed or failed generations up to the configured limit.
        # The final invalid result is still persisted for later inspection.
        for attempt in range(1, max_attempts + 1):
            try:
                response_text = generate_response(
                    platform,
                    prompt,
                    model_name,
                    generation_config,
                    client,
                    openai_response_format,
                )

                evaluation, last_validation_error = parse_and_validate_evaluation(
                    response_text,
                    protocol,
                )

            except Exception as exc:
                evaluation = {"error": "generation_error", "detail": str(exc)}

                last_validation_error = str(exc)
    
            if last_validation_error is None:
                break

            print(
                "[WARN] Invalid evaluation for "
                f"query {candidate_pool['id_query']}, "
                f"result {index}, "
                f"model {model_name} "
                f"(attempt {attempt}/{max_attempts}): "
                f"{last_validation_error}"
            )

        hit["annotations"][model_name] = evaluation

        # Persist after every annotation so the process can be resumed
        # without repeating completed evaluations.
        save_annotated_pool(annotated_pool, output_path)


def annotation_settings_from_config(config):
    """Read Stage 3 annotation settings from the benchmark config."""
    llm_config = config["llm_annotation"]

    return {
        "max_content": llm_config["content_character_limit"],
        "max_attempts": llm_config["maximum_attempts"],
        "few_shots": llm_config["few_shots"],
        "few_shot_examples": llm_config["few_shot_examples"],
        "blueprint": llm_config["prompt_blueprint"].strip(),
    }


def annotation_protocol_from_config(config):
    """Read Stage 3 annotation protocol from the benchmark config."""
    llm_config = config["llm_annotation"]

    return {
        "input_components": [
            component.strip()
            for component in llm_config["input_components"]
        ],
        "reason_labels": [
            reason.strip()
            for reason in llm_config["reason_labels"]
        ],
        "relevance_mapping": dict(llm_config["relevance_mapping"]),
        "positive_evidence_fields": [
            field.strip()
            for field in llm_config["positive_evidence_fields"]
        ],
        "negative_evidence_normalization": dict(
            llm_config["negative_evidence_normalization"]
        ),
    }


def parse_args():
    """Parse Stage 3 LLM-annotation command-line arguments."""
    parser = argparse.ArgumentParser(
        description="Annotate Stage 2 candidate pools with an LLM relevance judge."
    )

    parser.add_argument("--model", "-m", required=True, help="LLM model identifier.")
    parser.add_argument("--models-config", default="config/models.json", help="Model metadata used to validate model IDs.")
    parser.add_argument("--benchmark-config", default="config/benchmark_config.json", help="Benchmark configuration JSON used for annotation settings.")
    parser.add_argument("--input", "-i", default="data/candidate_pool", help="Directory containing Stage 2 candidate-pool JSON files.")
    parser.add_argument("--output", "-o", default="data/llm_annotations", help="Directory used to store annotated candidate pools.")

    return parser.parse_args()


def main():
    """Run Stage 3 annotation for one configured LLM judge."""
    args = parse_args()

    model_config_path = Path(args.models_config)

    benchmark_config_path = Path(args.benchmark_config)

    benchmark_config = load_benchmark_config(benchmark_config_path)

    annotation_config = annotation_settings_from_config(benchmark_config)
    protocol = annotation_protocol_from_config(benchmark_config)
    openai_response_format = build_openai_evaluation_response_format(protocol)

    models_config = load_models_config(model_config_path)

    # The benchmark config selects the experimental judges; models.json only
    # supplies backend and generation settings for those configured judges.
    models_by_id = {model["id"].strip(): model for model in models_config["models"]}

    annotation_models = [
        model_id.strip() for model_id in benchmark_config["llm_annotation"]["models"]
    ]

    missing_model_configs = sorted(set(annotation_models) - set(models_by_id))

    if missing_model_configs:
        raise ValueError(
            "Missing model configuration(s) for annotation judge(s): "
            f"{missing_model_configs}"
        )

    if args.model not in annotation_models:
        raise ValueError(
            f"Model {args.model!r} is not configured as an annotation judge. "
            f"Expected one of: {annotation_models}"
        )

    model_config = models_by_id[args.model]

    platform = platform_from_model_entry(model_config)

    generation_config = dict(model_config["generation"])

    if platform == "vertex":
        generation_config["response_mime_type"] = generation_config[
            "response_mime_type"
        ].strip()

    input_dir = Path(args.input).resolve()
    output_dir = Path(args.output).resolve()

    if not input_dir.is_dir():
        raise ValueError(
            f"Input directory does not exist: {input_dir}"
        )

    if input_dir == output_dir:
        raise ValueError(
            "Stage 3 input and output directories must be different."
        )

    output_dir.mkdir(
        parents=True,
        exist_ok=True,
    )

    # Freeze the effective annotation settings before processing any pair so
    # later invocations can verify that a resume is methodologically compatible.
    current_provenance = build_run_provenance(
        args.model,
        platform,
        annotation_config,
        generation_config,
        protocol,
    )

    ensure_run_provenance(output_dir, args.model, current_provenance, protocol)

    print("Starting LLM annotation with:")
    print(f" - Platform: {platform}")
    print(f" - Model: {args.model}")
    print(f" - Models configuration: {model_config_path}")
    print(f" - Benchmark configuration: {benchmark_config_path}")
    print(f" - Input directory: {input_dir}")
    print(f" - Output directory: {output_dir}")
    print(f" - Maximum content characters: {annotation_config['max_content']}")
    print(f" - Few-shot prompting: {annotation_config['few_shots']}")
    print(f" - Blueprint: {annotation_config['blueprint']}")
    print(
        f" - Input components: "
        f"{', '.join(protocol['input_components'])}"
    )
    print(f" - Maximum annotation attempts: {annotation_config['max_attempts']}")
    print("=" * 80)

    client = None

    if platform in ("openai", "gpt"):
        from openai import OpenAI

        api_key = os.getenv("OPENAI_API_KEY")

        if not api_key:
            raise ValueError("OPENAI_API_KEY is not configured.")

        client = OpenAI(api_key=api_key)

    elif platform in ("gemini", "vertex"):
        initialize_vertex_ai()

    input_files = list_query_json_files(input_dir)

    evidence_fields = dataset_input_fields(protocol)

    # Query files are processed in numeric query-ID order for deterministic
    # traversal; each annotation file is independently resumable.
    for input_path in tqdm(input_files, desc="Annotating query files"):
        candidate_pool = load_candidate_pool(input_path)

        output_path = output_dir / input_path.name

        annotated_pool = load_or_initialize_annotated_pool(
            candidate_pool, output_path, evidence_fields
        )

        if not candidate_pool["hits"]:
            save_annotated_pool(annotated_pool, output_path)

            print(f"No candidate results: {input_path.name}")

            continue

        print(
            f"\nProcessing {input_path.name}: "
            f"{len(candidate_pool['hits'])} "
            f"candidate pair(s)"
        )

        annotate_candidate_pool(
            platform=platform,
            candidate_pool=candidate_pool,
            annotated_pool=annotated_pool,
            model_name=args.model,
            generation_config=generation_config,
            client=client,
            protocol=protocol,
            openai_response_format=openai_response_format,
            output_path=output_path,
            max_content_chars=annotation_config["max_content"],
            few_shots=annotation_config["few_shots"],
            few_shot_examples=annotation_config["few_shot_examples"],
            blueprint=annotation_config["blueprint"],
            max_attempts=annotation_config["max_attempts"],
        )
    print("\nLLM annotation completed.")


if __name__ == "__main__":
    main()
